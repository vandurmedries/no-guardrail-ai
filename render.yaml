services:
  - type: web
    name: no-guardrail-ai-proxy
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python proxy.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
